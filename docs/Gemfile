# Local preview only: GitHub Pages (legacy builder) uses the github-pages gem, so pin the same here.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# No longer bundled with Ruby 3.x, but Jekyll 3 needs them.
gem "webrick"
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
gem "erb"
