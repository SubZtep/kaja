{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "stt": {
      "type": "object",
      "properties": {
        "speachesUrl": {
          "description": "Speaches AI server endpoint (speech-to-text)",
          "type": "string",
          "format": "uri"
        },
        "language": {
          "description": "Language hint for speech-to-text, e.g. 'en'",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "tts": {
      "type": "object",
      "properties": {
        "speachesUrl": {
          "description": "Speaches AI server endpoint (text-to-speech)",
          "type": "string",
          "format": "uri"
        },
        "voice": {
          "description": "Voice name to use for text-to-speech",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "memory": {
      "type": "object",
      "properties": {
        "dbPath": {
          "description": "Absolute path to the SQLite memory database; omit to use the default XDG data location",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "marketplace": {
      "description": "Fetching abilities from the online marketplace",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Use the online marketplace; false means Kaja never goes online for abilities (default: true)",
          "type": "boolean"
        },
        "autoFetch": {
          "description": "Pull the marketplace at startup when the last sync is over a day old (default: true)",
          "type": "boolean"
        }
      }
    },
    "context": {
      "description": "Keeping long conversations inside the model's context window",
      "type": "object",
      "properties": {
        "compact_at": {
          "description": "How full the chat model's context may get (0.3-0.95) before older messages are summarised (default: 0.8)",
          "type": "number",
          "minimum": 0.3,
          "maximum": 0.95
        }
      }
    },
    "preferences": {
      "description": "In-app preferences",
      "type": "object",
      "properties": {
        "thinking": {
          "description": "Show thinking indicator when the model is generating a response",
          "type": "boolean"
        },
        "sounds": {
          "description": "Enable sound effects",
          "type": "boolean"
        },
        "voice": {
          "description": "Enable voice output (text-to-speech)",
          "type": "boolean"
        },
        "locale": {
          "description": "Language for the chat and application",
          "type": "string",
          "enum": [
            "en-GB",
            "hu-HU",
            "nan-TW",
            "zh-TW"
          ]
        },
        "mode": {
          "description": "Which backend `kaja` starts: the cloud API, or the local agent against your own provider",
          "type": "string",
          "enum": [
            "cloud",
            "local"
          ]
        },
        "hotkeyModifier": {
          "description": "Modifier key for the help/persona hotkeys (default: alt)",
          "type": "string",
          "enum": [
            "alt",
            "ctrl"
          ]
        },
        "theme": {
          "description": "Colour theme; auto asks the terminal for its background colour (default: auto)",
          "type": "string",
          "enum": [
            "auto",
            "dark",
            "light"
          ]
        }
      }
    }
  }
}
