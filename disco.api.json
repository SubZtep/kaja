{
  "version": "1.0",
  "services": {
    "web": {
      "port": 3001,
      "image": "backend"
    },
    "hook:deploy:start:before": {
      "type": "command",
      "command": "for f in $(printf '%s\\n' ./apps/api/migrations/*.sql | LC_ALL=C sort); do psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f \"$f\"; done",
      "image": "backend"
    }
  },
  "images": {
    "backend": {
      "dockerfile": "apps/api/Dockerfile",
      "context": "."
    }
  }
}
