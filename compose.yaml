services:
  db:
    image: postgres:18.6-alpine3.24
    ports:
      - "127.0.0.1:5433:5432"
    restart: unless-stopped
    shm_size: 128mb
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: kaja
    volumes:
      - ./pgdata:/var/lib/postgresql
      - ./apps/api/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "testuser", "-d", "postgres"]
      start_period: 3s

  mail:
    image: maildev/maildev:3.0.0
    restart: unless-stopped
    ports:
      - "127.0.0.1:1080:1080"
      - "127.0.0.1:1025:1025" # expose SMTP to host
    environment:
      MAILDEV_MCP: 1
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://127.0.0.1:1080/index.html",
        ]
      timeout: 5s

  # S3-compatible object storage for images (Hetzner Object Storage in production); the API creates the bucket on boot
  storage:
    image: rustfs/rustfs:1.0.0
    restart: unless-stopped
    ports:
      - "127.0.0.1:9000:9000" # S3 API
      - "127.0.0.1:9001:9001" # console
    environment:
      RUSTFS_VOLUMES: /data
      RUSTFS_ADDRESS: 0.0.0.0:9000
      RUSTFS_CONSOLE_ADDRESS: 0.0.0.0:9001
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_ACCESS_KEY: kaja # matches apps/api/.env.example
      RUSTFS_SECRET_KEY: kaja-dev-storage
    volumes:
      - storage:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:9000/health"]
      start_period: 5s

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "127.0.0.1:3001:3001"
    depends_on:
      db:
        condition: service_healthy
      mail:
        condition: service_healthy
      storage:
        condition: service_healthy
    env_file:
      - path: apps/api/.env.example
        required: true
      - path: apps/api/.env
        required: false
    environment:
      DATABASE_URL: postgresql://testuser:testpass@db:5432/kaja
      SMTP_HOST: mail
      STORAGE_ENDPOINT: http://storage:9000
      STORAGE_PUBLIC_ENDPOINT: http://localhost:9000 # signed URLs name the host the browser and the TUI reach
      CORS_ORIGIN: http://localhost:3000
      BETTER_AUTH_SECRET: compose_dev_better_auth_secret_not_for_production # local stack only
      SANDBOX_URL: http://sandbox:3002
      SANDBOX_SECRET: compose_dev_sandbox_secret_not_for_production # local stack only
    networks:
      - default
      - sandbox
    # The image has no curl or wget, so Bun's fetch does the check.
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "process.exit((await fetch('http://127.0.0.1:3001/health')).ok ? 0 : 1)",
        ]
      timeout: 5s
      start_period: 10s

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      api:
        condition: service_healthy
    env_file:
      - path: apps/web/.env.example
        required: true
      - path: apps/web/.env
        required: false
    environment:
      API_URL: http://api:3001
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "process.exit((await fetch('http://127.0.0.1:3000/')).ok ? 0 : 1)",
        ]
      timeout: 5s
      start_period: 10s

  # Runs stdio MCP servers (a headless Chrome among them) for cloud turns. Only the api shares its network, so it can't
  # reach db or mail by name, and every port here is published on 127.0.0.1 only, so not through the host either.
  # Chrome also goes out only through the sandbox's egress proxy (public addresses only); production runs it on a separate host too.
  sandbox:
    build:
      context: .
      dockerfile: apps/sandbox/Dockerfile
    ports:
      - "127.0.0.1:3002:3002"
    shm_size: 1gb
    env_file:
      - path: apps/sandbox/.env.example
        required: true
      - path: apps/sandbox/.env
        required: false
    environment:
      SANDBOX_SECRET: compose_dev_sandbox_secret_not_for_production # local stack only
    networks:
      - sandbox
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3002/health').then(r => process.exit(r.ok ? 0 : 1), () => process.exit(1))",
        ]
      timeout: 5s
      start_period: 8s

networks:
  sandbox:

volumes:
  storage:
