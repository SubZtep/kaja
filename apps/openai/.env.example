KAJA_API_URL=https://api.kaja.io

# REQUIRED (fail-closed): same shared secret as apps/api CONFIG_API_TOKEN.
# Used both to call /config/models and to authorize inbound proxy traffic.
CONFIG_API_TOKEN=kaja

# Logger Configuration
KAJA_APP_NAME=openai
KAJA_LOG_LEVEL=trace
