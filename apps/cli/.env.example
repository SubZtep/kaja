# Baked into the compiled binary (Bun --env=API_*).
# Resolution order: --api-url > API_URL > env-paths config.json(apiUrl) > default.
API_URL=http://localhost:3001

# Logger Configuration
KAJA_APP_NAME=cli
KAJA_LOG_LEVEL=trace
